#include <stdio.h>

/**
 * main - Entry point
 * author: IamNaeto
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	int naeto;

	for (naeto = 0; naeto < 10; naeto++)
	{
		print("%d", naeto);
	}
	printf("\n");
	return (0);
}
