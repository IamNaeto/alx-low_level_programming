#include <stdio.h>

/**
 * main - Entry point
 * author: IamNaeto
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
	int naeto;

	for (naeto = 0 ; naeto < 10 ; naeto++)
	{
		printf("%d", naeto);
	}
	printf("\n");
	return (0);
}
